export class VuexyState {
}
export function Mutation(target, propertyKey, descriptor) {
}
export function Action(target, propertyKey, descriptor) {
}
export function Getter(target, propertyKey, descriptor) {
}
export class VuexyModule extends Object {
    static Instance(c) {
        let m = Vuexy.getModule(c);
        if (!m)
            m = new c();
        Vuexy.registerModule(m);
        return m;
    }
}
export class Vuexy {
    static init(store) {
        this.$_store = store;
    }
    static registerModule(m) {
        this.modules.push(m);
        let proto = Object.getPrototypeOf(m);
        let states = Object.getOwnPropertyNames(m);
        let funcs = Object.getOwnPropertyNames(proto);
        funcs.splice(funcs.indexOf('constructor'), 1);
    }
    static getModule(c) {
        this.modules.forEach(m => {
            if (m instanceof c)
                return m;
        });
        return null;
    }
}
Vuexy.modules = [];
Vuexy.$_store = null;
//# sourceMappingURL=index.js.map